// Smoke test placeholder — the full app requires Hive and platform initialization
// which cannot be done in a simple widget test without mocking.
// Feature-specific tests are in test/scoring/, test/models/, test/chat/.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — see feature-specific test files', () {
    expect(true, isTrue);
  });
}
