Now let me zoom in on options 3 and 4 to compare them carefully, including a rotation-alignment test.

---

The mirror hypothesis is confirmed geometrically (IoU 0.985 mirrored vs 0.816 rotation-only). Now the key question: is the difference visible to a normal candidate? Let me render the overlays.